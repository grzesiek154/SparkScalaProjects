package com.grzesiek.spark.book.exercises
import java.io.File
import org.apache.spark.SparkContext
import org.apache.log4j._

object test1 {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(184); 
  println("Welcome to the Scala worksheet");$skip(16); 
  
 val x = 1+3;System.out.println("""x  : Int = """ + $show(x ))}
}
