package rdd



object RddOperationsTest extends App{
  
  val someCollection = List(1,4,5,6)
  
  //to learn
  //aggregate and aggregateByKey
  
  //val asd = someCollection.aggregate((0,0))((s, r) => (s + r)) 
  
  
}